export class CustomButton {
    index :number;
    name : string;
    image : string;

    constructor(index : number, name: string, image: string) {
        this.index = index;
        this.name = name;
        this.image = image
    }
}